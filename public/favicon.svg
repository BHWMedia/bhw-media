<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <!-- Void dark background -->
  <rect width="32" height="32" rx="5" fill="#06060D"/>
  <!-- Top-right small square (bright blue) — exact match to navbar logo mark -->
  <rect x="17" y="4" width="10" height="10" rx="2" fill="#3B82F6"/>
  <!-- Bottom-left larger square (dark blue) — exact match to navbar logo mark -->
  <rect x="5" y="16" width="14" height="13" rx="2.5" fill="#1D4ED8"/>
</svg>
